# AI-Use Statement

This project was developed with AI assistance (Claude Code). This file records, honestly and
concretely, what was AI-assisted and how the results were verified.

## Tools

- **Claude Code** (Anthropic) — pair-programming: scaffolding, boilerplate, code review, docs.

## What was AI-assisted

| Area | AI role | How I verified / understood it |
|------|---------|--------------------------------|
| Repo scaffold, project structure, `.gitignore`, `pyproject.toml` | Generated by AI from my spec | Reviewed each file; ran `uv sync`. |
| `fmfs/data/datasets.py` (MNIST loader, 32x32, [-1, 1]) | Written by AI | Ran a smoke test: loaded one batch, checked shape and value range. |
| `fmfs/utils/seed.py` (seeding, device selection) | Written by AI | Read the code; confirmed deterministic seeding and correct device on Mac. |
| `fmfs/models/unet.py` (time/class-conditioned UNet) | Written by AI | Smoke test: forward pass shape + param count (~6.5M); traced the skip-connection bookkeeping myself. |
| `fmfs/flow/rectified_flow.py` (interpolation, velocity, Euler sampler, CFG) | Written by AI | Smoke test: loss backward, sampling at 1/2/8 steps; checked the interpolation/velocity math against the notes. |
| `fmfs/utils/ema.py`, `fmfs/utils/plotting.py` | Written by AI | Reviewed; verified EMA updates and that grids/curves render. |
| `scripts/train.py` (training loop, label dropout, EMA, checkpointing) | Written by AI | Ran a 60-step smoke train on MNIST: loss dropped, checkpoint + loss curve + sample grid produced. |
| `fmfs/flow/ddpm.py` (eps-prediction DDPM, DDIM/DDPM samplers) | Written by AI | Smoke test: loss backward, sampling at 1/2/8/50 steps, checked the DDIM update + eta (0=DDIM, 1=ancestral DDPM). |
| `scripts/sample.py`, `fmfs/inference.py` (multi-step grids, checkpoint loader) | Written by AI | Ran against a checkpoint; confirmed grids at several step counts with shared seed. |
| `fmfs/metrics/` + `scripts/eval_fid.py` (InceptionV3 FID + MNIST-FID, FID-vs-steps) | Written by AI | Smoke-ran both metrics end-to-end (json + plots); checked the Frechet-distance usage and the shared `.features()` interface. |
| `scripts/reflow.py` + coupled loss/coupling generator | Written by AI | Smoke-ran reflow (coupling -> retrain), confirmed the student checkpoint loads like any flow model; understood why reflowed pairs straighten the paths. |
| `notebooks/01_demo.ipynb`, `notebooks/02_results.ipynb` | Generated by AI | Validated as notebooks; they orchestrate the CLI scripts so the logic lives in reviewed modules, not the notebooks. |
| First Kaggle T4 run (debugging clone/gitignore/GPU issues) | AI drove the run + diagnosis | Read the kernel logs; diagnosed a gitignore that dropped `fmfs/data`, a package-name clash, and a P100/torch mismatch; verified the CFG-vs-steps artifact locally from the downloaded checkpoint. |
| `tests/test_basic.py` | Written by AI | Reviewed; run locally (7 tests pass) — shapes, sampler determinism, EMA, both methods, guidance path, trajectory shape. |
| `report/REPORT.md` skeleton | Drafted by AI | I own the analysis and wording; numbers filled from my own reading of `fid.json`. |

## What I did myself

- All design decisions (architecture size, conditioning, metrics, datasets, extras).
- Reviewing and understanding every module before committing it.

_Updated continuously as the project grows._
