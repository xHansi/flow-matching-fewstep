# On Kaggle, torch/torchvision/numpy/scipy/matplotlib/tqdm are preinstalled.
# `pip install -r requirements.txt` still works there (already-satisfied lines are skipped).
torch>=2.2
torchvision>=0.17
numpy>=1.26
scipy>=1.11
matplotlib>=3.8
tqdm>=4.66
pytorch-fid>=0.3.0
